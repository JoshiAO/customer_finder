class AppPrivateConfig {
  // Update these values before publishing a public build.
  static const String appName = 'Customer Finder';

  // Change launch logo/title by editing these asset/text values.
  static const String launchLogoAsset = 'assets/images/JoshiAO.jpg';
  static const String launchTitle = 'JoshiAO Project';

  // Keep private endpoints/keys here while developing private builds.
  // Do not rely on client-side code for real secret protection.
  static const String backendBaseUrl = 'https://your-private-api.example.com';
  static const String mapsApiKey = 'REPLACE_WITH_PRIVATE_KEY';
}
